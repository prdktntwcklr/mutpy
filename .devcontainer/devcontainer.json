{
    "name": "Python Multi-Version Dev",
    "build": {
        "dockerfile": "../Dockerfile",
        "args": {
            "PYTHON_VERSION": "3.13"
        }
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-python.vscode-pylance"
            ]
        }
    },
    // Ensures your local files are synced and you run as a non-root user if needed
    "remoteUser": "root",
    "postCreateCommand": "pip install pre-commit && pre-commit install",
    "containerEnv": {
        "EDITOR": "code --wait"
    },
}
